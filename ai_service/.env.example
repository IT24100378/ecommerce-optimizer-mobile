# Shared API key between backend and ai_service.
# Must match backend/.env -> AI_SERVICE_API_KEY
AI_SERVICE_API_KEY=dev-ai-service-key

# Keep false in normal usage. Set true only for temporary local bypass.
ALLOW_UNSAFE_NO_API_KEY=false

